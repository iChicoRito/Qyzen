Objective #1: Admin multiple upload for Students Accounts

    Paths Related to this objective:
    - src\app\(admin)\admin\users
    - src\app\(admin)\admin\users\components
    - src\app\(admin)\admin\users\data
    - src\app\(admin)\admin\users\page.tsx

    1). Here in the admin, I have a add user there for students and educator, but the focus of this objective is students

    2)  In the user management beside the (Add User) button, I wanted to add button for Upload File for uploading the students. As u can see, the current implementation is manual and 1 by 1 adding that it takes time for multiple users.

    3). For the referrence only, look at the (src\app\(educator)\educator\assessment\quizzes\components\add-quiz-modal.tsx) this is the modal for adding the quiz but now here in the admin side, I wanted the same layout for upload the files.

    4). Make sure it has downloadable excel template like on the educator but for uploading the users.