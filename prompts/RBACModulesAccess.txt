Objective #1: User Multi-Roles and Module Access

    1). In the current RBAC, the user can has an role right? Now I created a user that has a 2 roles. Now what I wanted that
        if that user has 2 roles, their sidebar will shows the modules they can access. for example the user has applied two
        roles, admin and educator, meaning the sidebar will shows all the modules that admin and educator has.

Instructions and Objective in JSON format for better readability:

    {
        "objectives": [
            {
            "objective_number": 1,
            "objective_title": "User Multi-Roles and Module Access",
            "tasks": [
                {
                "task_number": 1,
                "task_title": "Multi-Role Sidebar Module Aggregation",
                "context": "In the current RBAC implementation, a user can be assigned a role. The new requirement extends this to support users with multiple roles simultaneously.",
                "requirement": {
                    "description": "If a user has been assigned multiple roles, the sidebar must display the combined and aggregated list of all modules that each of those roles has access to",
                    "behavior": "union",
                    "example": {
                    "user_assigned_roles": [
                        "admin",
                        "educator"
                    ],
                    "expected_result": "The sidebar shows all modules that admin has access to plus all modules that educator has access to"
                    }
                },
                "relevant_existing_tables": [
                    {
                    "table_name": "tbl_user_roles",
                    "role": "Source of all roles assigned to the user"
                    },
                    {
                    "table_name": "tbl_roles",
                    "role": "Role definitions"
                    },
                    {
                    "table_name": "tbl_role_permissions",
                    "role": "Mapping of roles to their permissions and modules"
                    }
                ]
                }
            ]
            }
        ]
        }