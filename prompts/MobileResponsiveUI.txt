Objective #1: Fix all the non-reponsive UI and make it mobile compatible

    1).  Path: src\app\(educator)

        a). In these paths, all the UI for desktop is already fine but in mobile view it isnt

            - src\app\(educator)\educator\assessment
            - src\app\(educator)\educator\classroom
            - src\app\(educator)\educator\dashboard
            - src\app\(educator)\educator\enrollment

        b). Most of the page shows the "Please use a larger screen to view the full module management interface." whenever I'm on
            the mobile view.

        c). Fix all the pages in the educator pages, tables and some of the overriding UI elements that will be compatible
            for mobile.

    2). Path: src\app\(admin)

        a). In these paths, all the UI for desktop is already fine but in mobile view it isnt

            - src\app\(admin)\admin\academic-settings
            - src\app\(admin)\admin\access-control
            - src\app\(admin)\admin\dashboard
            - src\app\(admin)\admin\users

        b). Most of the page shows the "Please use a larger screen to view the full module management interface." whenever I'm on
            the mobile view.

        c). Fix all the pages in the educator pages, tables and some of the overriding UI elements that will be compatible
            for mobile.

    3). Before you proceed to the execution, give a summary of the process, what are the plans and what codes will
        be modified or added and delete (risky) and wait for the "Go!" command.

Objective #2: Actual Quiz page of student in dekstop and mobile view

    1). As u can see on the image, in the desktop view, it's all fine, the floating timer can be
        hidden and show and also can be dragged anywhere I want.

    2). Now in the mobile view, in the image above, as u can see, it is like covering almost the screen
        what kind of plan do you have to make the timer more compact but still noticable.

    3). Before you proceed to the execution, give a summary of the process, what are the plans and what codes will
        be modified or added and delete (risky) and wait for the "Go!" command.
    