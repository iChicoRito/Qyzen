Objective: Refractor User Management in Admin Side

    Paths Related to this Objective:
    - src\app\(admin)\admin\users

    1). In the user management, I want to add delete flow to this and uses the same delete-confirmation-modal like the others, it will delete the user in the database both public.tbl_users and auth.users for that same user

    2). In the context menu, it has view, edit and delete, add for "Resend Email" for resending the verification email to that user