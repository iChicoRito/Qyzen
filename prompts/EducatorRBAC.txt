Objective #1: Section Module

    1). In the educator side, I want to apply the rbac I have, not the educator can do anything on the section and not
        relying on the rbac.

    2). I wanted that the educator can do based on their permissions on their role.

    3). Before you proceed to the execution, give a summary of the process, what codes will be
        modified or added and delete (risky) and wait for the "Go!" command.

Objective #2: Subject Module

    1). In the educator side, I want to apply the rbac I have, not the educator can do anything on the section and not
        relying on the rbac.

    2). I wanted that the educator can do based on their permissions on their role.

    3). Before you proceed to the execution, give a summary of the process, what codes will be
        modified or added and delete (risky) and wait for the "Go!" command.

Instructions and Objective in JSON format for better readability:

    {
    "prompt": {
        "objectives": [
        {
            "id": "Objective #1",
            "module": "Section Module",
            "tasks": [
            {
                "task_number": 1,
                "description": "In the educator side, apply the RBAC that is already in place. The educator must not be able to do anything on the section by default, and the system must not bypass or ignore the existing RBAC."
            },
            {
                "task_number": 2,
                "description": "The educator must only be able to perform actions based on the permissions defined in their assigned role within the existing RBAC system."
            },
            {
                "task_number": 3,
                "instruction": "Before proceeding to execution, provide a summary of the process. The summary must include: what codes will be modified, what codes will be added, and what codes will be deleted (flagged as risky). After the summary is provided, wait for the 'Go!' command before proceeding."
            }
            ]
        },
        {
            "id": "Objective #2",
            "module": "Subject Module",
            "tasks": [
            {
                "task_number": 1,
                "description": "In the educator side, apply the RBAC that is already in place. The educator must not be able to do anything on the subject by default, and the system must not bypass or ignore the existing RBAC."
            },
            {
                "task_number": 2,
                "description": "The educator must only be able to perform actions based on the permissions defined in their assigned role within the existing RBAC system."
            },
            {
                "task_number": 3,
                "instruction": "Before proceeding to execution, provide a summary of the process. The summary must include: what codes will be modified, what codes will be added, and what codes will be deleted (flagged as risky). After the summary is provided, wait for the 'Go!' command before proceeding."
            }
            ]
        }
        ],
        "global_rules": {
        "pre_execution_requirement": "For every objective, a summary must be presented before any code changes are made. Execution only begins after the 'Go!' command is received.",
        "summary_must_include": [
            "Files or code sections to be modified",
            "Files or code sections to be added",
            "Files or code sections to be deleted, each flagged explicitly as risky"
        ],
        "rbac_enforcement": "The existing RBAC system must be used as the authority for all permission checks. No hardcoded permissions or role bypasses are allowed on the educator side.",
        "scope": "Changes apply to the educator-facing side only, for both the Section Module and Subject Module."
        }
    }
    }