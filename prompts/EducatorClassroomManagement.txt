Objective #1: Creation of Section (src\app\(educator)\educator\classroom\sections)

    1). The whole (src\app\(educator)\educator\classroom) is copied from the template, I wanted to change the
        contents of all this files.

    2). Here in this page, I would like to make as section creation. Before that, create a new table name "tbl_sections"
        and must have these columns:

        - id
        - educator_id          (fk -> tbl_users.id)
        - academic_term_id     (fk -> tbl_academic_term.id) this will be the added by educator
        - section_name
        - is_active

        Also create a junction table for that since it will be many-to-many relatonship. The table name "tbl_sections_term"

        - id
        - section_id
        - academic_term_id

    3). Change the "add-task-modal.tsx" to "add-section-modal.tsx". This modal contains the field of:

        - Section Name
        - Academic Term (checkboxes) it can be multiple
        - Status

        The adding of the section name should be unqiue, if it's already existing on that term, it won't be created. For example
        I added BSIT12M1 in Prelim, I cannot add BSIT12M1 again in the Prelim but in other selected terms it can be.

    4). The modal design for add, edit, and view should be like in the:

        - src\app\(admin)\admin\users\components\add-user-modal.tsx
        - src\app\(admin)\admin\users\components\edit-user-modal.tsx
        - src\app\(admin)\admin\users\components\view-user-modal.tsx

        This is only layout referrences. I want the modal should like that style and design.

    5). Before you proceed to the execution, give a summary of the process, what codes will be
        modified or added and delete (risky) and wait for the "Go!" command.

    6). Finish the Objective #1 first and that's a pause and wait for the Go command again if we're going to continue for 
        objective #2.

Objective #2: Creation of Subject (src\app\(educator)\educator\classroom\subjects)

    1). Here in this page, I would like to make as subject creation. Before that, create a new table name "tbl_subjects"
        and must have these columns:

        - id
        - educator_id          (fk -> tbl_users.id)
        - sections_id          (fk -> tbl_sections_id) this will be the added by educator
        - subject_code
        - subject_name
        - is_active

    2). Change the "add-task-modal.tsx" to "add-subject-modal.tsx". This modal contains the field of:

        - Subject Code
        - Subject Name
        - Section (dropdown but it will allowed multiple select) so educator can add 1 subject in multiple sections
        - status

        The subject code and name should be unique per section only.

    3). The modal design for add, edit, and view should be like in the:

        - src\app\(admin)\admin\users\components\add-user-modal.tsx
        - src\app\(admin)\admin\users\components\edit-user-modal.tsx
        - src\app\(admin)\admin\users\components\view-user-modal.tsx

        This is only layout referrences. I want the modal should like that style and design.

    4). Before you proceed to the execution, give a summary of the process, what codes will be
        modified or added and delete (risky) and wait for the "Go!" command.